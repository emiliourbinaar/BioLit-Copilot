# Copy to .env and fill in the value. .env is gitignored; this file is not.
# Read by `uv run --env-file .env python -m biolit_evals.extract_eval ...`
# Only the LLM arm needs this. The control arms make no API call.
ANTHROPIC_API_KEY=
