# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/
# note: backend/uv.lock is intentionally tracked (reproducible installs)
# note: backend/.python-version is intentionally tracked too. It was ignored here, so nothing
# pinned the interpreter: a clean `uv sync` resolved 3.14 locally while CI used 3.12.

# Node / Next.js (frontend)
node_modules/
.next/
out/
.turbo/
.vercel
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environments & secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Local agent / dev state (not for the public repo)
.claude/
.superpowers/
